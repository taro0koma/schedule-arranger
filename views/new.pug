extends layout

block content
  h1.my-5 MAKE A SCHEDULE
  p 予定を作成すると公開されます。候補日程は複数作成可能です。
  form(action="/schedules", method="post").my-3
    input(type="hidden", name="_csrf" value!=csrfToken)
    div.form-group
      label(for="scheduleName") 予定名
      input(type="text" name="scheduleName")#scheduleName.form-control
    div.form-group
      label(for="memo") メモ
      textarea(name="memo", cols="10", rows="3")#memo.form-control
    div.form-group
      label(for="candidates") 候補日程 ※ 日付けごとの改行が必要です
      textarea(name="candidates", rows="4" style="width:240px;")#candidates.form-control
    button(type="submit").btn.btn-info.my-3.btn-lg.btn-block 予定をつくる
